# ---- Supabase (same project as web/ and mobile/) ----
NEXT_PUBLIC_SUPABASE_URL=            # Supabase → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_ANON_KEY=       # Supabase → Settings → API → anon public key

# ---- Mapbox (live map on the Track screen) ----
# Public token — safe to expose. Same token family as the mobile app's
# EXPO_PUBLIC_MAPBOX_TOKEN. Without it, the Track screen shows the stylised
# fallback map instead of a live Mapbox map.
NEXT_PUBLIC_MAPBOX_TOKEN=

# ---- Web Push (VAPID) ----
# Only the PUBLIC half lives here (the applicationServerKey passed to
# pushManager.subscribe). The private half is a JWK-pair JSON stored ONLY
# as the VAPID_KEYS Supabase edge secret, read by send-push — the Next app
# never sees it. WEB_PUSH_CONTACT is likewise an edge secret.
NEXT_PUBLIC_VAPID_PUBLIC_KEY=
