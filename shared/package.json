{
  "name": "@busbuzz/shared",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone test scope for shared/ — not an npm workspace member (matches existing repo layout; only web/ and mobile/ are workspaces). Devs run tests with `cd shared && npm install && npm test`.",
  "scripts": {
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^29.5.14",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3",
    "zod": "^3.23.8"
  }
}
